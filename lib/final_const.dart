void main() {
  print("Why so serious?");

  // final name = "Rex"; //inline final variable
  // name = "Rox"; //can not assign value to the final variable after assigning a value
  //
  // print(name);

  // final var city = "Dhaka"; //can not assign var in final

  final String city;

  city = "Dhaka";
  city = "Khulna"; //we can assign value to final only once

  print(city);
}